import { ModelDirective } from './model.directive';

describe('ModelDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelDirective();
    expect(directive).toBeTruthy();
  });
});
